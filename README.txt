感光材料设备和串口数据的收发

1. 每一个设备有对应的ID，拿起任何一个设备上的标签时，感光材料接收到光照，对串口发送ID信号，程序将相应的ID写入txt文件。
2. 如果不拿起任何标签或拿起多个标签，写入txt文件的字符串将为"default"，也就是说，只有拿起一个标签的时候才能正确触发相应视频。
3. 模板字符串为signal.txt在电脑中文件夹里的路径，signal.txt文件需要与signal_write.py脚本在同一个文件夹中。
4. 开始播放视频之后，打开signal_write.py脚本，此时拿起或放下标签会导致signal.txt文件中内容改变，进而跳转视频。